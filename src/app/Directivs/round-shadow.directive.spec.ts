import { RoundShadowDirective } from './round-shadow.directive';

describe('RoundShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundShadowDirective();
    expect(directive).toBeTruthy();
  });
});
